# .kiro — כלים

כאן חיים agents, skills, hooks ו-steering. **זה לא תוכן.** התוכן חי מחוץ ל-`.kiro/` וקריא בלי Kiro — לכל סוכן, לכל כלי, ולבן אדם ב-GitHub.

- `steering/` — מצביע על `00-התוכנית/` ו-`03-ידע/`. לא מכפיל אותם.
- `agents/`, `skills/`, `hooks/` — לפי המבנה של אסף.
